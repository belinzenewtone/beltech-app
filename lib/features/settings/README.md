# settings feature

## Purpose
Provides app preference controls such as security and appearance options.

## Main classes
- Presentation: `lib/features/settings/presentation/`
- Domain: `lib/features/settings/domain/`
- Data: `lib/features/settings/data/`

## Dependencies
Uses `authProvider` for biometric security preferences and status.

