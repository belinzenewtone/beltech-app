# BELTECH Logo Asset

Place your official logo file here as:

`assets/branding/beltech_logo.jpeg`

The app already references this path in `BeltechLogo.assetPath`.
If the file is missing, the UI falls back to a generic icon.
